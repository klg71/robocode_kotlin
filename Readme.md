# Robocode Kotlin examples

## Prerequisite
Set `robocodeDir` in ~/.gradle/gradle.properties

## Build

You need to update RoboCop.properties to match you robots classname

run `./gradlew install` to install robot
